build:
  gpu: true
  python_version: "3.10"
  system_packages:
    - git
    - nvidia-cuda-toolkit
    - ffmpeg
    - libsm6
    - libxext6
    - libxrender-dev
    - libglib2.0-0
  python_requirements: requirements.txt

predict: "predict.py:Predictor"   